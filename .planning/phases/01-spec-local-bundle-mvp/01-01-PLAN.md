---
phase: 01-spec-local-bundle-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/spec/bundle-json.md
  - docs/spec/lineage-policy.md
  - docs/spec/cli-surface.md
  - docs/spec/tool-path-mapping.md
  - schemas/bundle-1.0.0.schema.json
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - TOOL-01
must_haves:
  truths:
    - "A reader can implement bundle.json and versioning behavior without reading other chat logs."
    - "Import/lineage policy and required lineage identifiers are explicit and stable in-repo."
    - "Phase 1 CLI command surface and core→tool path mapping are documented for local/remote consumers."
  artifacts:
    - path: docs/spec/bundle-json.md
      provides: "Human normative bundle.json + schema_version rules (D-01–D-03)"
    - path: schemas/bundle-1.0.0.schema.json
      provides: "Machine-readable JSON Schema for schema_version 1.0.0 bundle documents"
    - path: docs/spec/lineage-policy.md
      provides: "SPEC-02 + D-04 field list and import copy semantics"
    - path: docs/spec/cli-surface.md
      provides: "SPEC-03 + D-12–D-16 command names and behavioral notes"
    - path: docs/spec/tool-path-mapping.md
      provides: "TOOL-01 Cursor/Codex path rules table (spec-only)"
  key_links:
    - from: schemas/bundle-1.0.0.schema.json
      to: docs/spec/bundle-json.md
      via: "Doc references schema file path; field names align"
---

<objective>
Fix schema, versioning, lineage/import policy, CLI command surface, and tool mapping documents in the repository so Phase 1+ implementations can trace every rule to SPEC/TOOL IDs and decisions D-01–D-16.
</objective>

<context>
@.planning/phases/01-spec-local-bundle-mvp/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/superpowers/specs/2026-03-31-claude-code-bundle-platform-design.md
</context>

## Verification criteria (plan-level)

1. `grep -r` / `rg` checks in acceptance criteria for each task all pass on a clean tree.
2. `schemas/bundle-1.0.0.schema.json` is valid JSON and declares `$schema` if using draft 2020-12 (or omit `$schema` but document which draft in `bundle-json.md`).
3. No deferred Phase 2 items (remote account list, URL-only install) appear as Phase 1 requirements in these docs.

<tasks>

<task type="auto">
<name>Task 1: bundle.json doc + JSON Schema (SPEC-01, D-01–D-03)</name>
<files>docs/spec/bundle-json.md, schemas/bundle-1.0.0.schema.json</files>
<objective>
Author normative `bundle.json` documentation and JSON Schema for `schema_version` **1.0.0** (per D-01).
</objective>
<read_first>
- `.planning/phases/01-spec-local-bundle-mvp/01-CONTEXT.md`
- `.planning/REQUIREMENTS.md`
- `docs/superpowers/specs/2026-03-31-claude-code-bundle-platform-design.md`
</read_first>
<action>
Create **`docs/spec/bundle-json.md`** that:
- Defines the top-level manifest/bundle document name (`bundle.json` or documented equivalent), **`schema_version` starting value `1.0.0` (D-01)**.
- Documents **semantic versioning consumption rules**: **major mismatch → processors MUST fail with non-zero exit** (D-02); **minor/patch mismatch → warn and continue** (D-03).
- Lists required/optional fields needed for Phase 1 (at minimum: bundle identity, `visibility` enum used by lint policy D-10, manifest/payload pointers as needed for pack/unpack later).
- Links to **`schemas/bundle-1.0.0.schema.json`** as the machine-readable definition.

Create **`schemas/bundle-1.0.0.schema.json`** describing the same `schema_version: "1.0.0"` document shape (types for documented fields; use `"const": "1.0.0"` or equivalent for `schema_version` where appropriate).

Do **not** invent server/API fields beyond what SPEC-01 needs for local bundle MVP; Phase 2 entities stay out or clearly marked “future phase”.
</action>
<acceptance_criteria>
- `test -f docs/spec/bundle-json.md && test -f schemas/bundle-1.0.0.schema.json`
- `rg -n '1\.0\.0|D-01' docs/spec/bundle-json.md`
- `rg -n 'major|non-zero|exit|D-02' docs/spec/bundle-json.md`
- `rg -n 'warn|minor|patch|D-03' docs/spec/bundle-json.md`
- `python3 -m json.tool schemas/bundle-1.0.0.schema.json > /dev/null` (or `node -e "JSON.parse(require('fs').readFileSync('schemas/bundle-1.0.0.schema.json'))"` if node available)
</acceptance_criteria>
<verify>
  <automated>test -f docs/spec/bundle-json.md && test -f schemas/bundle-1.0.0.schema.json && rg -n 'D-01|D-02|D-03' docs/spec/bundle-json.md && python3 -c 'import json; json.load(open("schemas/bundle-1.0.0.schema.json"))'</automated>
</verify>
<done>bundle-json.md and JSON Schema exist; D-01–D-03 documented and schema parses</done>
</task>

<task type="auto">
<name>Task 2: lineage and import policy (SPEC-02, D-04)</name>
<files>docs/spec/lineage-policy.md</files>
<objective>
Document import/private-copy lineage policy and explicit lineage identifier fields (D-04, SPEC-02).
</objective>
<read_first>
- `.planning/phases/01-spec-local-bundle-mvp/01-CONTEXT.md` (sections D-04, Deferred)
- `docs/spec/bundle-json.md` (after Task 1 exists)
</read_first>
<action>
Create **`docs/spec/lineage-policy.md`** stating **SPEC-02** verbatim policy: import creates a **new private copy**; **no automatic upstream sync**; **lineage records snapshot relationships only**.

Include a **field inventory (D-04)** with exact names:
`origin_bundle_id`, `origin_snapshot_id`, `imported_at`, `root_bundle_id`, `root_author_id`, `parent_bundle_id` — each with 1–2 sentences on meaning and Phase 1 local vs Phase 2+ persistence.

Explicitly **exclude** deferred ideas from CONTEXT: remote account bundle list on `list`, install without URL from account, third-party install via URL (list them under “Out of scope for Phase 1”).
</action>
<acceptance_criteria>
- `rg -n 'origin_bundle_id|origin_snapshot_id|imported_at|root_bundle_id|root_author_id|parent_bundle_id' docs/spec/lineage-policy.md`
- `rg -n 'private copy|no automatic|upstream|snapshot' docs/spec/lineage-policy.md`
- `rg -n 'Deferred|Phase 1|Out of scope' docs/spec/lineage-policy.md`
</acceptance_criteria>
<verify>
  <automated>test -f docs/spec/lineage-policy.md && rg -n 'origin_bundle_id|parent_bundle_id' docs/spec/lineage-policy.md && rg -n 'upstream|private copy' docs/spec/lineage-policy.md</automated>
</verify>
<done>Lineage fields D-04 and SPEC-02 semantics documented; Phase 1 deferrals explicit</done>
</task>

<task type="auto">
<name>Task 3: CLI surface + tool mapping (SPEC-03, TOOL-01, D-12–D-16)</name>
<files>docs/spec/cli-surface.md, docs/spec/tool-path-mapping.md</files>
<objective>
Define Phase 1 CLI/API command surface (SPEC-03) and core→Cursor/Codex path mapping table (TOOL-01), aligned with D-12–D-16.
</objective>
<read_first>
- `.planning/phases/01-spec-local-bundle-mvp/01-CONTEXT.md` (D-12–D-16, D-13 pack gating)
- `docs/spec/bundle-json.md`
- `docs/spec/lineage-policy.md`
</read_first>
<action>
Create **`docs/spec/cli-surface.md`** listing **official Phase 1 commands (D-12)**: `create`, `pack`, `unpack`, `apply`, `install`, `list`, `lint`, `manifest validate` (document exact spelling including subcommand vs space).

For each command, add one short paragraph: purpose, primary inputs/outputs, and relevant decisions:
- **D-13**: `pack` MUST fail with guidance if no valid manifest (manual flow); CI may pass explicit manifest path.
- **D-14**: `create` is terminal multi-step (numeric selection).
- **D-07–D-08**: `apply` target root `~/.claude/...`; default fail on conflict; `--force` overwrite.
- **D-15–D-16**: `install` from local archive/path only; updates local registry; `list` reads local registry + install snapshot state.

Note future **sync/pull/push** as reserved names for Phase 2+ without implementing them.

Create **`docs/spec/tool-path-mapping.md`** (TOOL-01): a **table** mapping **core bundle components** (e.g. skills, hooks, commands, templates) → **Claude Code paths under `~/.claude/`** → **Cursor** and **Codex** destination paths or doc links column (use “TBD + link to official docs” where path unknown; table must exist with ≥4 rows covering distinct component types).

Cross-reference `bundle-json.md` field names where mappings attach to manifest sections.
</action>
<acceptance_criteria>
- `rg -n 'create|pack|unpack|apply|install|list|lint|manifest validate' docs/spec/cli-surface.md`
- `rg -n 'D-1[2-6]|~/.claude' docs/spec/cli-surface.md`
- `rg -n 'SPEC-03|TOOL-01|\|.*\|.*\|' docs/spec/cli-surface.md docs/spec/tool-path-mapping.md`
- `wc -l docs/spec/tool-path-mapping.md | awk '$1>=15 {exit 0} {exit 1}'` (table doc materially non-trivial)
</acceptance_criteria>
<verify>
  <automated>test -f docs/spec/cli-surface.md && test -f docs/spec/tool-path-mapping.md && rg -n 'manifest validate|D-13|~/.claude' docs/spec/cli-surface.md && test "$(wc -l docs/spec/tool-path-mapping.md | awk '{print $1}')" -ge 15</automated>
</verify>
<done>cli-surface.md lists D-12 commands and D-13–D-16 notes; tool-path-mapping.md has ≥15 lines</done>
</task>

</tasks>

## Success criteria

- SPEC-01, SPEC-02, SPEC-03, and TOOL-01 are satisfied by files under `docs/spec/` and `schemas/` per REQUIREMENTS.md.
- ROADMAP success criterion #1 is unblocked: another implementer can derive `bundle.json` + lineage + CLI surface from committed docs alone.

## Output

After execution, create `.planning/phases/01-spec-local-bundle-mvp/01-01-SUMMARY.md` per GSD template.
