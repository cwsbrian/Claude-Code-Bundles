---
phase: 01-spec-local-bundle-mvp
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/cli/index.ts
  - src/lib/apply.ts
  - src/lib/lint.ts
  - src/lib/registry.ts
  - src/lib/manifest-validate.ts
  - src/lib/create-wizard.ts
  - tests/apply-lint-e2e.test.ts
  - tests/lint.test.ts
  - tests/fixtures/secret-samples/**/*
  - scripts/e2e-local.sh
autonomous: true
requirements:
  - CLI-03
  - CLI-04
must_haves:
  truths:
    - "`apply` writes files under ~/.claude/... layout per tool mapping doc and conflicts fail without --force (D-07, D-08)."
    - "`lint` scans full archive content locally (D-09); public bundle + high-confidence secret → non-zero; private → warn only (D-10); filename patterns included (D-11)."
    - "Automated E2E covers create → pack → lint → apply → list on a temp HOME."
  artifacts:
    - path: src/lib/apply.ts
      provides: "Claude path materialization"
    - path: src/lib/lint.ts
      provides: "Local secret/risk scan without LLM (D-09)"
    - path: tests/apply-lint-e2e.test.ts
      provides: "Local E2E scenario"
    - path: scripts/e2e-local.sh
      provides: "One-command local reproduction"
  key_links:
    - from: src/lib/lint.ts
      to: bundle visibility
      via: "visibility: public vs private gates exit code D-10"
    - from: src/lib/apply.ts
      to: docs/spec/tool-path-mapping.md
      via: "Paths must match documented Claude targets"
---

<objective>
Implement `apply`, `lint`, `manifest validate`, `create` wizard, `install`, `list`; add secret sample fixtures; run a temp-HOME E2E proving CLI-03, CLI-04, and ROADMAP criteria 3–4.
</objective>

<context>
@.planning/phases/01-spec-local-bundle-mvp/01-CONTEXT.md
@docs/spec/cli-surface.md
@docs/spec/tool-path-mapping.md
@src/lib/pack.ts
@src/lib/unpack.ts
</context>

## Verification criteria (plan-level)

1. `npx vitest run` includes `tests/apply-lint-e2e.test.ts` passing with **isolated `HOME`** (env var override).
2. `bash scripts/e2e-local.sh` exits 0 on developer machine (uses same CLI entry as tests).
3. `lint` tests demonstrate: **private** + synthetic API key → **warning path** still exit 0 for apply/pack flow if spec says warn-only; **public** + same → **non-zero** (D-10).

<tasks>

<task type="auto">
<name>Task 1: manifest validate, create, install, list, apply</name>
<files>src/cli/index.ts, src/lib/manifest-validate.ts, src/lib/create-wizard.ts, src/lib/registry.ts, src/lib/apply.ts</files>
<objective>
Implement `manifest validate`, `create` (D-14), `install` (D-15), `list` (D-16), and `apply` (D-07, D-08) against `~/.claude` layout.
</objective>
<read_first>
- `.planning/phases/01-spec-local-bundle-mvp/01-CONTEXT.md`
- `docs/spec/cli-surface.md`
- `docs/spec/tool-path-mapping.md`
- `src/cli/index.ts`
</read_first>
<action>
Add **`src/lib/manifest-validate.ts`**: validate given JSON file against `schemas/bundle-1.0.0.schema.json`; CLI subcommand **`manifest validate <file>`** prints structured errors to stderr and exits 1 on failure.

Add **`src/lib/create-wizard.ts`**: stdin/stdout multi-step prompts: bundle name → **visibility** (`public`|`private`) → checklists for skills/hooks/commands/templates using numeric menus (D-14); writes **`bundle.json`** (or name from spec) to cwd.

Add **`src/lib/registry.ts`**: local JSON registry file under **`$HOME/.claude/bundle-platform/registry.json`** (exact path documented in module comment — Claude discretion per CONTEXT) tracking installed bundle id, last snapshot id, and paths.

Add **`src/lib/apply.ts`**:
- Resolves target **root** to `path.join(homedir(), '.claude', ...)` per **`docs/spec/tool-path-mapping.md` Claude column** (create parent dirs).
- **D-08**: if target file exists and `--force` not set → exit non-zero with clear message; with `--force`, overwrite.

Wire **`install`**: input = local archive path; unpack to temp; `apply`; update **registry** (D-15).

Wire **`list`**: read registry; print bundle id + installed snapshot + paths (D-16).

Extend **`src/cli/index.ts`** with subcommands: `create`, `apply`, `install`, `list`, `manifest validate`.
</action>
<acceptance_criteria>
- `rg -n 'manifest validate|create|install|list|apply|--force' src/cli/index.ts`
- `rg -n '\.claude|homedir|registry' src/lib/apply.ts src/lib/registry.ts`
- `npm run build` exits 0
- `node dist/cli/index.js manifest validate schemas/bundle-1.0.0.schema.json` is NOT valid test; instead: `node dist/cli/index.js manifest validate tests/fixtures/golden-bundle/manifest.json` **or** the actual fixture filename returns 0
</acceptance_criteria>
<verify>
  <automated>npm run build && rg -n 'apply|registry|manifest validate' src/cli/index.ts</automated>
</verify>
<done>validate/install/list/apply/create wired per D-14–D-16 and D-07–D-08</done>
</task>

<task type="auto">
<name>Task 2: lint engine (CLI-04, D-09–D-11)</name>
<files>src/lib/lint.ts, src/cli/index.ts, tests/lint.test.ts</files>
<objective>
Implement local-only `lint` scanning archive contents with public=block / private=warn rules (D-09–D-11).
</objective>
<read_first>
- `.planning/phases/01-spec-local-bundle-mvp/01-CONTEXT.md` (D-09–D-11)
- `src/lib/pack.ts`
</read_first>
<action>
Add **`src/lib/lint.ts`** that:
- Accepts archive path + manifest (or reads manifest from archive metadata if embedded — **choose one** and document).
- Walks **every file in archive** (D-11: not manifest-only).
- Applies **high-confidence** rules (Claude discretion): minimum set must include regex for `sk-[A-Za-z0-9]{20,}` style key **or** `AKIA` AWS id (pick concrete patterns in code comments) **and** high-risk basename checks: `.env`, `*.pem`, `id_rsa`, `credentials.json` (D-11).

**D-10**: Read `visibility` from manifest:
- `public` + any high-confidence hit → **exit 1** from `lint` CLI.
- `private` + hits → **stderr warnings**, **exit 0**.

**D-09**: No LLM calls; pure local scan.

Wire **`lint` subcommand** in `src/cli/index.ts`.
</action>
<acceptance_criteria>
- `rg -n 'visibility|public|private|sk-|AKIA|\.env|pem|id_rsa|credentials\.json|D-09|D-10|D-11' src/lib/lint.ts`
- Unit test file **`tests/lint.test.ts`** (add if not present) passes: `npx vitest run tests/lint.test.ts`
</acceptance_criteria>
<verify>
  <automated>test -f src/lib/lint.ts && npx vitest run tests/lint.test.ts</automated>
</verify>
<done>lint blocks public secrets, warns private per D-10; tests pass</done>
</task>

<task type="auto">
<name>Task 3: secret fixtures + apply/lint E2E + e2e-local.sh</name>
<files>tests/fixtures/secret-samples/, tests/apply-lint-e2e.test.ts, scripts/e2e-local.sh, package.json</files>
<objective>
Add secret sample fixtures and full local E2E: temp HOME, create → pack → lint → apply → list.
</objective>
<read_first>
- `tests/pack-unpack.test.ts`
- `src/lib/create-wizard.ts`
- `src/lib/lint.ts`
</read_first>
<action>
Create **`tests/fixtures/secret-samples/`**:
- `private-with-secret/bundle.json` (visibility `private`) + tiny file containing a **synthetic** fake API key matching lint regex (not a real key).
- `public-with-secret/bundle.json` (visibility `public`) + same pattern file.

Add **`tests/apply-lint-e2e.test.ts`**:
- Sets `process.env.HOME` to `os.tmpdir()` subdir (or uses `spawn` with env) for isolation.
- **MUST exercise `create` first (D-14)** in the primary path: non-interactive mode (e.g. env `BUNDLE_CLI_NONINTERACTIVE=1` with fixed answers, or heredoc/stdin script documented in test comment) producing a valid `bundle.json` in the temp cwd, **then** `manifest validate` on that file, **then** `pack`, **then** `lint` on the produced archive, **then** `apply --force`, **then** `list` shows a registry entry. A second test case may use the `public-with-secret` / `private-with-secret` fixtures only for lint exit-code assertions (D-10); the main E2E line must still include `create` so the flow matches `must_haves`.

Add **`scripts/e2e-local.sh`**: `set -euo pipefail`; `npm run build`; runs the **same ordered steps** as the Vitest E2E (`create` → `manifest validate` → `pack` → `lint` → `apply` → `list`); echoes OK.

Update **`package.json`** script `"test:e2e": "bash scripts/e2e-local.sh"` optional.
</action>
<acceptance_criteria>
- `test -f tests/fixtures/secret-samples/public-with-secret/bundle.json`
- `test -f tests/fixtures/secret-samples/private-with-secret/bundle.json`
- `npx vitest run tests/apply-lint-e2e.test.ts` exits 0
- `bash scripts/e2e-local.sh` exits 0
- `rg -n 'HOME|tmp|e2e|create|lint|apply' tests/apply-lint-e2e.test.ts scripts/e2e-local.sh`
</acceptance_criteria>
<verify>
  <automated>npx vitest run tests/apply-lint-e2e.test.ts && bash scripts/e2e-local.sh</automated>
</verify>
<done>E2E and shell script prove CLI-03/CLI-04 on isolated HOME</done>
</task>

</tasks>

## Success criteria

- CLI-03: documented Claude `~/.claude/...` tree exists after `apply` in E2E.
- CLI-04: lint blocks public+secret sample and warns on private+secret sample.
- ROADMAP success criteria #3–#4 satisfied with automated evidence.

## Output

After execution, create `.planning/phases/01-spec-local-bundle-mvp/01-03-SUMMARY.md`.
