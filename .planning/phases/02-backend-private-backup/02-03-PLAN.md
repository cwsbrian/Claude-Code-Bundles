---
phase: 02-backend-private-backup
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - packages/cli/src/index.ts
  - packages/cli/src/remote.ts
  - packages/cli/package.json
  - packages/cli/README.md
  - scripts/e2e-backup-restore.sh
  - tests/remote-backup-restore.e2e.test.ts
  - docs/spec/cli-surface.md
autonomous: true
requirements:
  - API-01
  - API-02
  - SEC-01
must_haves:
  truths:
    - "`ccb remote upload` (D-16) sends Bearer token and multipart archive to deployed (or `next dev`) upload API; succeeds for valid bundle."
    - "Same machine (single device): download via API + unpack/apply reproduces prior install per existing CLI behavior."
    - "Automated or scripted check proves **another user cannot list or download** the first user's bundle (SEC-01 / RLS + API guards)."
  artifacts:
    - path: packages/cli/src/remote.ts
      provides: "Auth + multipart upload client; optional list/download helpers for E2E"
    - path: scripts/e2e-backup-restore.sh
      provides: "One-shot backup → clear local registry/temp → restore flow"
    - path: tests/remote-backup-restore.e2e.test.ts
      provides: "Programmatic SEC-01 negative + happy path behind env gate"
  key_links:
    - from: packages/cli/src/remote.ts
      to: apps/web upload route
      via: "HTTPS base URL from env `CCB_API_URL` or flag"
    - from: tests/remote-backup-restore.e2e.test.ts
      to: Supabase second user
      via: "create second session or anon+wrong token expecting 403/404 on list/download"
---

<objective>
Ship **`packages/cli`** `ccb remote upload` (D-16) against `apps/web` APIs; add a **single-device backup→restore** E2E (script and/or Vitest) that exercises API-01/API-02 end-to-end; add **SEC-01** verification (cross-account or RLS-violating request must not leak metadata or bytes).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-backend-private-backup/02-CONTEXT.md
@apps/web/src/app/api/bundles/upload/route.ts
@packages/cli/src/index.ts
@docs/spec/cli-surface.md
</context>

<tasks>

<task type="auto">
<name>Task 1: CLI remote upload + optional list/download helpers</name>
<files>packages/cli/src/remote.ts, packages/cli/src/index.ts, packages/cli/package.json, docs/spec/cli-surface.md</files>
<objective>
Implement D-16: subcommand **`ccb remote upload`** with flags: **`--archive`**, **`--api-url`** (or env `CCB_API_URL`), **`--token`** (or env `SUPABASE_ACCESS_TOKEN` / `CCB_ACCESS_TOKEN` — document one canonical name); POST multipart field name **`archive`** matching server (D-06). Print JSON response ids/hashes on success; stderr + non-zero on 4xx with server error body when JSON.
</objective>
<read_first>
- `.planning/phases/02-backend-private-backup/02-CONTEXT.md` (D-16, D-03)
- `apps/web/src/app/api/bundles/upload/route.ts` (field names)
</read_first>
<action>
1. Add **`packages/cli/src/remote.ts`**: functions `uploadArchive({ apiUrl, token, archivePath })` using `fetch` + `FormData` + `File`/`Blob` from Node 20+ or `fs.createReadStream` with correct multipart library if needed; set `Authorization: Bearer ${token}`.
2. Wire **`remote upload`** in **`packages/cli/src/index.ts`**; parse flags; call upload; exit codes: 0 success, 1 user error, 2 network/5xx (pick and document).
3. Optionally add **`remote list`** / **`remote download`** thin wrappers calling GET list and GET download — **if** needed by E2E script to avoid curl; keep in same module.
4. Update **`docs/spec/cli-surface.md`** with `remote` subcommands and env vars.
</action>
<acceptance_criteria>
- `rg -n "remote upload|CCB_API_URL|FormData|Authorization" packages/cli/src`
- `npx nx build cli && node dist/cli/index.js remote upload --help` (or help text path matching build output)
</acceptance_criteria>
<verify>
  <automated>npx nx build cli && rg -n "remote upload" packages/cli/src/index.ts</automated>
</verify>
<done>Upload subcommand built; docs updated.</done>
</task>

<task type="auto">
<name>Task 2: Single-device E2E + SEC-01 negative test</name>
<files>scripts/e2e-backup-restore.sh, tests/remote-backup-restore.e2e.test.ts, package.json</files>
<objective>
Prove roadmap Phase 2 success criteria on **one machine**: pack → remote upload → list (CLI or API) → download archive → unpack/apply → verify filesystem/registry expectation; prove **SEC-01** with a second access token (different Supabase user) that **cannot** list or download the first user's bundle_id/snapshot_id (expect 403/404).
</objective>
<read_first>
- `.planning/ROADMAP.md` Phase 2 success criteria
- `scripts/e2e-local.sh` (pattern for temp HOME)
- `apps/web/src/app/api/bundles/route.ts` and download route paths
</read_first>
<action>
1. Add **`scripts/e2e-backup-restore.sh`** (bash): export temp `HOME`; run `ccb create`/`pack`/`remote upload` with env from `.env.local` / user-exported vars (`CCB_API_URL`, token); call list endpoint (curl or `node` using small fetch script if no `remote list`); download snapshot to temp file; run `ccb install` or unpack+apply per existing E2E; assert file exists (golden path flexible); exit non-zero on failure.
2. Add **`tests/remote-backup-restore.e2e.test.ts`**: gate with `process.env.CCB_E2E_INTEGRATION === '1'` (or similar); when set, run against real API; when unset, **skip** integration but run **SEC-01 unit** using mocked `fetch` or route handler if available. **Must include** one automated test that **without gate** validates pure logic (e.g. wrong bearer → client does not send wrong bearer twice) OR document `MISSING` — prefer: mock server returns 403 and CLI exits non-zero.
3. Add **`npm run test:e2e:remote`** (or extend `test:e2e`) in root `package.json` invoking the bash script.
4. Document required env vars at top of **`scripts/e2e-backup-restore.sh`** comments.
</action>
<acceptance_criteria>
- `test -x scripts/e2e-backup-restore.sh || head -5 scripts/e2e-backup-restore.sh`
- `rg -n "CCB_E2E|403|SEC-01|second.*user" tests/remote-backup-restore.e2e.test.ts`
- `grep -n "e2e-backup-restore" package.json`
</acceptance_criteria>
<verify>
  <automated>npx nx test cli && npm run build</automated>
</verify>
<done>E2E script exists; automated test file demonstrates SEC-01 negative path; default CI runs unit tests without staging secrets.</done>
</task>

</tasks>

<verification>
Manual once: run `scripts/e2e-backup-restore.sh` against preview deployment with two Supabase test users; confirm second user gets forbidden on list/download.
</verification>

<success_criteria>
1. Same account: backup (upload) and restore (download + apply) demonstrated on one device.
2. SEC-01: other user's token cannot access private bundle metadata or archive via API.
3. CLI surface documents remote commands and env vars.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-private-backup/02-03-SUMMARY.md`
</output>
