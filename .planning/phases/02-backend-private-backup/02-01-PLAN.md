---
phase: 02-backend-private-backup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nx.json
  - package.json
  - package-lock.json
  - tsconfig.base.json
  - apps/web/project.json
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.mjs
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - packages/core/project.json
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/src/index.ts
  - packages/core/src/manifest-validate.ts
  - packages/core/src/snapshot-hash.ts
  - packages/core/src/lint.ts
  - packages/core/src/pack.ts
  - packages/core/src/unpack.ts
  - packages/core/src/apply.ts
  - packages/core/src/registry.ts
  - packages/core/src/create-wizard.ts
  - packages/cli/project.json
  - packages/cli/package.json
  - packages/cli/tsconfig.json
  - packages/cli/src/index.ts
  - vitest.config.ts
  - tests/pack-unpack.test.ts
  - tests/lint.test.ts
  - tests/apply-lint-e2e.test.ts
  - scripts/e2e-local.sh
  - supabase/migrations/*.sql
  - supabase/config.toml
  - docs/backend/storage-bucket-policy.md
  - .env.example
autonomous: true
requirements:
  - BE-01
  - BE-02
  - SEC-01
must_haves:
  truths:
    - "`nx run-many -t test --all` (or documented equivalent) runs existing Phase 1 tests green after code moves to `packages/core` / `packages/cli`."
    - "Postgres migration defines `bundles`, `bundle_snapshots`, `bundle_lineage` with owner-scoped RLS so anon and other users cannot read another user's rows."
    - "R2 namespace and server-only write policy are documented; bucket does not grant public/anonymous client write."
  artifacts:
    - path: packages/core
      provides: "Shared manifest validate, snapshot hash, pack/unpack, lint, apply helpers (migrated from root `src/lib`)"
    - path: packages/cli
      provides: "`ccb` entry migrated from root `src/cli`; bin points at built output"
    - path: apps/web
      provides: "Minimal Next.js App Router shell for Phase 2 follow-up API routes"
    - path: supabase/migrations/
      provides: "BE-01 tables + SEC-01 RLS policies"
    - path: docs/backend/storage-bucket-policy.md
      provides: "BE-02 bucket naming, `{userId}/...` key convention sketch, server-only upload note"
  key_links:
    - from: packages/cli
      to: packages/core
      via: "workspace dependency `@claude-code-bundles/core` (or chosen package name)"
    - from: supabase/migrations/*.sql
      to: auth.users
      via: "owner_user_id FK or uuid matching Supabase Auth user id"
---

<objective>
Establish the Nx monorepo boundary (D-01, D-02): migrate Phase 1 `src/lib` → `packages/core` and `src/cli` → `packages/cli` without breaking automated tests; add minimal `apps/web` Next.js shell; deliver Supabase SQL migrations for `bundles` / `bundle_snapshots` / `bundle_lineage` (BE-01), RLS for private owner isolation (SEC-01), and an **R2** bucket policy sketch doc (BE-02).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-backend-private-backup/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/superpowers/specs/2026-03-31-claude-code-bundle-platform-design.md
@schemas/bundle-1.0.0.schema.json
</context>

<tasks>

<task type="auto">
<name>Task 1: Nx workspaces + migrate core/cli + green tests</name>
<files>nx.json, package.json, tsconfig.base.json, packages/core/**, packages/cli/**, apps/web/**, vitest.config.ts, tests/*.ts, scripts/e2e-local.sh</files>
<objective>
Complete Wave 1 migration per strict path assignments in CONTEXT (D-01, D-02): single source for schema path (fix either `packages/core` embedding `schemas/` copy or root `schemas/` re-export via build — pick one and document in `packages/core` README comment only if needed).
</objective>
<read_first>
- `.planning/phases/02-backend-private-backup/02-CONTEXT.md` (D-01, D-02, D-12)
- `package.json`, `tsconfig.json`, `vitest.config.ts`
- `src/lib/*.ts`, `src/cli/index.ts`
</read_first>
<action>
1. Add **Nx** at repo root: `nx.json`, root `package.json` **workspaces** (`apps/*`, `packages/*`), `tsconfig.base.json` with path mappings (e.g. `@claude-code-bundles/core` → `packages/core/src`).
2. Create **`packages/core`**: move all modules from `src/lib/` into `packages/core/src/`; export public API from `packages/core/src/index.ts` (manifest validate, snapshot hash, pack/unpack, lint, apply, registry, create-wizard as needed by CLI/tests).
3. Create **`packages/cli`**: move `src/cli/index.ts` to `packages/cli/src/index.ts`; set `package.json` **bin** `ccb` → dist output; depend on `packages/core`.
4. Create **`apps/web`**: scaffold **Next.js App Router** (App Router + TypeScript); minimal `layout.tsx` / `page.tsx` so `nx build web` succeeds; no API routes in this task.
5. Rewrite **imports** in `tests/pack-unpack.test.ts`, `tests/lint.test.ts`, `tests/apply-lint-e2e.test.ts` to use `@claude-code-bundles/core` and invoke CLI from `packages/cli` build output (or `tsx` entry consistent with Nx target).
6. Add Nx targets: `build`, `test` for `core` and `cli`; `build` for `web`. Remove or shim root `src/` so no duplicate sources remain (delete old `src/` after migration).
7. Update **`scripts/e2e-local.sh`** and **`vitest.config.ts`** paths to the new package layout.
</action>
<acceptance_criteria>
- `rg -n "packages/core|@claude-code-bundles/core" package.json packages/core/package.json packages/cli/package.json tsconfig.base.json`
- `test -f packages/core/src/manifest-validate.ts && test ! -d src/lib`
- Documented command: `npx nx run-many -t test --all` passes (or `npx nx test core` && `npx nx test cli` if split).
</acceptance_criteria>
<verify>
  <automated>npx nx run-many -t test --all && npm run build</automated>
</verify>
<done>Phase 1 tests pass; monorepo builds; `ccb` resolves from `packages/cli`; old root `src/lib` and `src/cli` removed.</done>
</task>

<task type="auto">
<name>Task 2: Supabase migrations + RLS + R2 policy sketch</name>
<files>supabase/migrations/*.sql, supabase/config.toml, docs/backend/storage-bucket-policy.md, .env.example, apps/web/package.json</files>
<objective>
Implement BE-01 schema (bundles, bundle_snapshots, bundle_lineage), SEC-01 RLS (no cross-user reads/writes on private bundle metadata), and BE-02 documentation for **Cloudflare R2** (private bucket, server-only S3 API writes) and `{userId}/...` keys (D-09–D-11, D-15).
</objective>
<read_first>
- `.planning/REQUIREMENTS.md` (BE-01, BE-02, SEC-01)
- `docs/superpowers/specs/2026-03-31-claude-code-bundle-platform-design.md` §8
- `.planning/phases/02-backend-private-backup/02-CONTEXT.md` (D-09, D-10, D-15)
</read_first>
<action>
1. Add **`supabase/migrations/`** SQL (timestamped file):
   - **`bundles`**: `id` (uuid PK), `owner_user_id` (uuid, not null), `visibility` (text/check constraint default `private`), `created_at`, `updated_at`, optional `display_name`.
   - **`bundle_snapshots`**: `id`, `bundle_id` FK, `normalized_snapshot_hash` (unique per bundle or global per product rules — document), `storage_object_key`, `schema_version`, `created_at`; FK to `bundles`.
   - **`bundle_lineage`**: `bundle_id` PK or unique, `parent_bundle_id`, `root_bundle_id`, `imported_snapshot_id` nullable (Phase 2 may insert nulls for local-created bundles).
   - Enable **RLS** on all three; policies: **SELECT/INSERT/UPDATE/DELETE** allowed only when `auth.uid() = owner_user_id` (for `bundles.owner_user_id`): for child tables, join through `bundle_id` to parent `bundles.owner_user_id = auth.uid()`. Use PostgreSQL idioms Supabase documents (e.g. `auth.uid()`).
2. Add **`supabase/config.toml`** if using Supabase CLI locally (optional linking pattern).
3. Write **`docs/backend/storage-bucket-policy.md`**: R2 bucket name convention, **no client direct write**, objects under `{userId}/...`, **Vercel Route Handlers** use **R2 API token** server-side; Supabase **service role** for Postgres only (D-04).
4. Add **`.env.example`** at repo root listing Supabase + **R2** env vars (no real secrets).
</action>
<acceptance_criteria>
- `rg -n "bundles|bundle_snapshots|bundle_lineage|ENABLE ROW LEVEL SECURITY|CREATE POLICY" supabase/migrations`
- `rg -n "R2|bucket|service.role|userId" docs/backend/storage-bucket-policy.md`
- `grep -E "SUPABASE|NEXT_PUBLIC" .env.example`
</acceptance_criteria>
<verify>
  <automated>rg -n "CREATE TABLE bundles|CREATE POLICY|bundle_snapshots" supabase/migrations && test -f docs/backend/storage-bucket-policy.md</automated>
</verify>
<done>Migrations apply cleanly on fresh DB; RLS policies documented in SQL comments; storage doc committed.</done>
</task>

</tasks>

<verification>
Full phase success also requires 02-02 and 02-03; for this plan: monorepo tests green + migration file(s) + RLS + storage sketch present.
</verification>

<success_criteria>
1. Nx migration complete; no regressions in pack/unpack/lint/apply E2E coverage.
2. BE-01 tables exist as SQL migration; SEC-01 enforced via RLS on bundle metadata tables.
3. BE-02 bucket policy documented for server-side writes only.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-private-backup/02-01-SUMMARY.md`
</output>
