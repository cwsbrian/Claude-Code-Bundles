---
phase: 01-spec-local-bundle-mvp
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - package.json
  - tsconfig.json
  - src/cli/index.ts
  - src/lib/pack.ts
  - src/lib/unpack.ts
  - src/lib/snapshot-hash.ts
  - tests/fixtures/golden-bundle/manifest.json
  - tests/fixtures/golden-bundle/**/*
  - tests/pack-unpack.test.ts
  - vitest.config.ts
autonomous: true
requirements:
  - CLI-01
  - CLI-02
must_haves:
  truths:
    - "`pack` produces an archive from a workspace with a valid manifest."
    - "`unpack` + normalization yields the same snapshot fingerprint as the golden fixture."
    - "Golden fixture test proves round-trip equivalence per D-05/D-06 (not raw zip bytes)."
  artifacts:
    - path: src/lib/snapshot-hash.ts
      provides: "Normalized content hash per D-05/D-06"
    - path: tests/fixtures/golden-bundle/
      provides: "Committed golden payload + manifest for regression"
    - path: tests/pack-unpack.test.ts
      provides: "Automated proof of CLI-02"
  key_links:
    - from: src/lib/pack.ts
      to: src/lib/snapshot-hash.ts
      via: "Hash excludes mtime/zip metadata per D-06"
    - from: tests/pack-unpack.test.ts
      to: tests/fixtures/golden-bundle/
      via: "Fixture path stable in repo"
---

<objective>
Implement Node.js + TypeScript `pack` and `unpack` CLI commands with normalized snapshot hashing (D-05, D-06) and a golden fixture test proving CLI-01 and CLI-02.
</objective>

<context>
@.planning/phases/01-spec-local-bundle-mvp/01-CONTEXT.md
@docs/spec/bundle-json.md
@docs/spec/cli-surface.md
@schemas/bundle-1.0.0.schema.json
</context>

## Verification criteria (plan-level)

1. `npm test` (or `npx vitest run`) completes in &lt; 60s and includes pack/unpack equality assertion.
2. `pack` and `unpack` subcommands exist on the CLI binary name chosen in `package.json` (e.g. `bundle-cli` or `ccb`).
3. Hash implementation explicitly documents exclusion of mtime, zip header timestamps, compression level, and OS metadata from comparison (D-06).

<tasks>

<task type="auto">
<name>Task 1: package.json + tsconfig + CLI pack/unpack stubs</name>
<files>package.json, tsconfig.json, src/cli/index.ts, vitest.config.ts</files>
<objective>
Scaffold root `package.json`, TypeScript, Vitest, and CLI entry exposing `pack` / `unpack` stubs.
</objective>
<read_first>
- `.planning/phases/01-spec-local-bundle-mvp/01-01-PLAN.md`
- `docs/spec/cli-surface.md`
- `docs/spec/bundle-json.md`
</read_first>
<action>
At repo root, add **`package.json`** with:
- `type: module` (or CommonJS — but prefer ESM + `"module": "NodeNext"` for portability per planning_context).
- `bin` pointing to compiled `dist/cli/index.js` **or** `tsx`/node launcher documented in README stub comment in package.json description field.
- Scripts: `build` (tsc), `test` (vitest run), `dev` optional.

Add **`tsconfig.json`** targeting modern Node LTS, `outDir` `dist`, `rootDir` `src`.

Add **`src/lib/pack.ts`** and **`src/lib/unpack.ts`** as minimal compilable stubs (e.g. `export async function pack(...){ throw new Error('unimplemented'); }`) so Task 1 ends with a green `npm run build`.

Add **`src/cli/index.ts`** parsing argv with a minimal dispatcher:
- `pack --manifest <path> --out <archive>` (exact flags can be `pack -m -o`; document in code comment).
- `unpack --archive <path> --out <dir>`.

**D-13**: If `--manifest` missing or file invalid vs `schemas/bundle-1.0.0.schema.json` (use `ajv` or lightweight JSON parse + manual check for Phase 1), `pack` MUST `process.exit(1)` with message mentioning `create` / manifest path.

Add **`vitest.config.ts`** with Node environment.

Task 1 MUST leave **hashing and real archive I/O** to Task 2; stubs may throw or no-op inside lib functions until Task 2 implements them.
</action>
<acceptance_criteria>
- `test -f package.json && test -f tsconfig.json && test -f src/cli/index.ts && test -f src/lib/pack.ts && test -f src/lib/unpack.ts && test -f vitest.config.ts`
- `rg -n '"pack"|"unpack"|bin' package.json src/cli/index.ts`
- `npm install` succeeds; `npm run build` exits 0 at end of Task 1 (compilable stubs only)
</acceptance_criteria>
<verify>
  <automated>test -f package.json && test -f src/cli/index.ts && rg -n pack src/cli/index.ts && npm install && npm run build</automated>
</verify>
<done>CLI entry and build pipeline exist; pack/unpack parsed</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: snapshot-hash + pack/unpack libraries (D-05, D-06)</name>
<files>src/lib/snapshot-hash.ts, src/lib/pack.ts, src/lib/unpack.ts, src/cli/index.ts, tests/pack-unpack.test.ts</files>
<objective>
Implement normalized snapshot hashing and archive pack/unpack excluding non-content metadata (D-06).
</objective>
<read_first>
- `.planning/phases/01-spec-local-bundle-mvp/01-CONTEXT.md` (D-05, D-06)
- `src/cli/index.ts`
</read_first>
<action>
Add **`src/lib/snapshot-hash.ts`** exporting a function e.g. `computeSnapshotId(files: Iterable<{ relativePath: string; content: Buffer }>): string` using:
- Sorted **Unicode NFC** path order (document in file header).
- Content: SHA-256 (or SHA-256 of concatenated `path + '\0' + content` per entry — **pick one**, document it) deterministically.
- Explicit comment: **mtime, zip central directory timestamps, compression level, OS-specific archive metadata are excluded** (D-06).

Add **`src/lib/pack.ts`**: given manifest + workspace root, include **only files declared / present per manifest rules** from `bundle-json.md`; write **`zip` or `tar.gz`** (choose one; document) where stored paths are relative POSIX paths.

Add **`src/lib/unpack.ts`**: read archive to memory map of `relativePath → content` without surfacing zip timestamps into hash input.

Wire **`src/cli/index.ts`** to call these libs.

**tests/pack-unpack.test.ts**: before implementation, add failing test sketch if following RED; otherwise ensure tests pass after GREEN.
</action>
<acceptance_criteria>
- `rg -n 'mtime|compression|D-06|normalize' src/lib/snapshot-hash.ts`
- `npm run build` exits 0
- `npx vitest run tests/pack-unpack.test.ts -t "snapshot hash is deterministic for fixed inputs"` exits 0 (test must assert a fixed expected digest string for two explicit paths/contents)
</acceptance_criteria>
<verify>
  <automated>rg -n 'D-06|mtime' src/lib/snapshot-hash.ts && npm run build && npx vitest run tests/pack-unpack.test.ts -t "snapshot hash is deterministic for fixed inputs"</automated>
</verify>
<done>Deterministic hash + pack/unpack; unit test green</done>
</task>

<task type="auto">
<name>Task 3: golden fixture + CLI-02 round-trip assertion</name>
<files>tests/fixtures/golden-bundle/, tests/pack-unpack.test.ts</files>
<objective>
Add golden fixture and prove pack→unpack snapshot id matches expected CLI-02 behavior (D-05).
</objective>
<read_first>
- `src/lib/snapshot-hash.ts`
- `src/lib/pack.ts`
- `src/lib/unpack.ts`
- `schemas/bundle-1.0.0.schema.json`
</read_first>
<action>
Create **`tests/fixtures/golden-bundle/`** with:
- `manifest.json` (or `bundle.json` if that is the committed name in docs) **valid** against `schemas/bundle-1.0.0.schema.json`.
- At least **2** nested text files with distinct relative paths and fixed UTF-8 content (no random bytes).

Extend **`tests/pack-unpack.test.ts`** to:
1. `pack` fixture dir to a temp archive path.
2. `unpack` to a second temp dir.
3. Compute snapshot id via **exported** `computeSnapshotId` (or internal test helper) on unpacked files and assert equals a **golden expected hash string** committed in test or fixture `EXPECTED_SNAPSHOT_ID.txt`.

Optional robustness: repack unpacked output to a **second** archive and assert **same** snapshot id (proves stability across zip round-trip metadata).

Document in test comment: equality is **normalized snapshot** (D-05), not byte-identical archives.
</action>
<acceptance_criteria>
- `test -f tests/fixtures/golden-bundle/manifest.json || test -f tests/fixtures/golden-bundle/bundle.json`
- `npx vitest run tests/pack-unpack.test.ts` exits 0
- `node dist/cli/index.js pack` (or `npx tsx src/cli/index.ts pack`) against fixture exits 0 when wired in test or manual script documented in test
- `rg -n 'CLI-02|D-05|golden' tests/pack-unpack.test.ts`
</acceptance_criteria>
<verify>
  <automated>test -d tests/fixtures/golden-bundle && npx vitest run tests/pack-unpack.test.ts</automated>
</verify>
<done>Golden fixture proves pack→unpack normalized snapshot equality</done>
</task>

</tasks>

## Success criteria

- CLI-01 and CLI-02 from REQUIREMENTS.md are satisfied with automated tests.
- ROADMAP success criterion #2 is satisfied: golden fixture proves pack→unpack snapshot equivalence.

## Output

After execution, create `.planning/phases/01-spec-local-bundle-mvp/01-02-SUMMARY.md`.
