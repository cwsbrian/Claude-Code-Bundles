---
phase: 02-backend-private-backup
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/web/src/lib/supabase/admin.ts
  - apps/web/src/lib/supabase/auth.ts
  - apps/web/src/app/api/bundles/upload/route.ts
  - apps/web/src/app/api/bundles/route.ts
  - apps/web/src/app/api/bundles/[bundleId]/snapshots/route.ts
  - apps/web/src/app/api/bundles/[bundleId]/snapshots/[snapshotId]/download/route.ts
  - packages/core/src/server-scan.ts
  - packages/core/src/index.ts
  - packages/core/package.json
  - apps/web/package.json
  - tests/api-upload.test.ts
  - vitest.config.ts
autonomous: true
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - "Authenticated multipart upload to `/api/...` persists archive via service role and records snapshot metadata for the token's user only."
    - "List and download endpoints return only the caller's private bundles/snapshots; download streams bytes from Storage server-side (D-08 preferred)."
    - "Invalid manifest or server scan failures return HTTP 4xx with structured errors and human-facing remediation text (API-01, D-12–D-14)."
  artifacts:
    - path: apps/web/src/app/api/bundles/upload/route.ts
      provides: "Multipart `archive` field; validate manifest via `packages/core`; server secret scan; Storage put; idempotent hash reuse (D-11)"
    - path: apps/web/src/app/api/bundles/route.ts
      provides: "GET list of caller's bundles + current snapshot summary"
    - path: apps/web/src/app/api/bundles/[bundleId]/snapshots/[snapshotId]/download/route.ts
      provides: "Authenticated download stream (API-02)"
    - path: apps/web/src/lib/supabase/
      provides: "Server client with `SUPABASE_SERVICE_ROLE_KEY`; JWT verification from `Authorization: Bearer` (D-03, D-04)"
  key_links:
    - from: upload/route.ts
      to: packages/core
      via: "import validate + scan; single source D-12"
    - from: upload/route.ts
      to: Supabase Storage
      via: "service role upload after ownership check"
    - from: list/download routes
      to: Postgres RLS
      via: "queries use authenticated user or service role with explicit owner filter matching JWT sub"
---

<objective>
Implement Vercel-facing Next.js Route Handlers in `apps/web` for private backup: **multipart upload** (API-01, D-06–D-07, D-12–D-14), **list** and **download stream** (API-02, D-08), using `packages/core` for manifest validation and server-side archive scan; enforce caller identity via Supabase JWT (D-03–D-05) and service-role server access only (D-04).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-backend-private-backup/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@supabase/migrations/*.sql
@packages/core/src/manifest-validate.ts
@packages/core/src/lint.ts
</context>

<tasks>

<task type="auto">
<name>Task 1: Supabase server helpers + auth for Route Handlers</name>
<files>apps/web/src/lib/supabase/admin.ts, apps/web/src/lib/supabase/auth.ts, apps/web/package.json</files>
<objective>
Add server-only clients: verify `Authorization: Bearer <access_token>` with Supabase (D-03); expose a helper that returns `user.id` or 401; separate **service role** client for Storage/DB writes that never imports into client bundles (D-04).
</objective>
<read_first>
- `.planning/phases/02-backend-private-backup/02-CONTEXT.md` (D-03–D-05)
- `apps/web/package.json`
</read_first>
<action>
1. Add **`apps/web/src/lib/supabase/admin.ts`**: create Supabase client using `SUPABASE_SERVICE_ROLE_KEY` + `NEXT_PUBLIC_SUPABASE_URL`; use only from Route Handlers / server code.
2. Add **`apps/web/src/lib/supabase/auth.ts`**: parse `Authorization` header; call `supabase.auth.getUser(jwt)` with anon client **or** `auth.getUser` pattern documented for Next.js Route Handlers; export `requireUser(request): Promise<{ id: string }>` throwing mapped 401 response.
3. Add dependencies **`@supabase/supabase-js`** (and `@supabase/ssr` only if needed) to `apps/web/package.json`.
4. Document env vars in root `.env.example` if not already present from 02-01.
</action>
<acceptance_criteria>
- `rg -n "SUPABASE_SERVICE_ROLE_KEY|createClient|getUser|Authorization" apps/web/src/lib/supabase`
- `npx nx build web` exits 0
</acceptance_criteria>
<verify>
  <automated>npx nx build web && rg -n "requireUser|createClient" apps/web/src/lib/supabase</automated>
</verify>
<done>Server can resolve authenticated user id from Bearer token; service role client isolated to server files.</done>
</task>

<task type="auto">
<name>Task 2: POST upload — validate, scan, Storage, DB, idempotent hash</name>
<files>apps/web/src/app/api/bundles/upload/route.ts, packages/core/src/server-scan.ts, packages/core/src/index.ts, tests/api-upload.test.ts</files>
<objective>
Implement API-01: `multipart/form-data` field **`archive`** (D-06); extract zip in memory/temp; validate `bundle.json` with existing schema helper (D-12); run **server secret scan** aligned with Phase 1 lint — **high-confidence findings reject upload always** (Phase 2, D-13); on success write Storage object under `{userId}/...` (D-10); insert/update `bundles` + `bundle_snapshots`; if same **normalized_snapshot_hash** exists for that bundle, skip new binary **reference existing snapshot** (D-11). Response JSON includes fixed field names (choose and document): e.g. `bundleId`, `snapshotId`, `normalizedSnapshotHash`, `storageObjectKey` (D-07).
</objective>
<read_first>
- `packages/core/src/lint.ts`, `packages/core/src/manifest-validate.ts`, `packages/core/src/snapshot-hash.ts`
- `.planning/phases/02-backend-private-backup/02-CONTEXT.md` (D-06, D-11–D-14)
</read_first>
<action>
1. Add **`packages/core/src/server-scan.ts`** (or extend `lint.ts` with exported `scanArchiveForSecrets(buffer|path): { ok: boolean; findings: ... }`) — **single implementation** reused by upload route; no duplicate regex tables.
2. Implement **`POST` `apps/web/src/app/api/bundles/upload/route.ts`**:
   - Call `requireUser`.
   - Parse multipart (`formData` / `request.formData()`).
   - Validate manifest; on failure **400/422** with JSON `{ code, message, details?, remediation? }` (D-14, API-01).
   - On scan failure: **400** with remediation strings (API-01).
   - Use **service role** to `storage.from(bucket).upload` with deterministic key naming.
   - Insert DB rows consistent with **02-01 migration** columns; enforce `owner_user_id = user.id`.
3. Add **`tests/api-upload.test.ts`**: mock Storage/DB or use **Supabase local** if configured — minimum: unit-test **pure functions** (hash idempotency decision) plus route tests with mocked `createClient` where full integration is unavailable.
</action>
<acceptance_criteria>
- `rg -n "formData|archive|multipart|upload" apps/web/src/app/api/bundles/upload/route.ts`
- `rg -n "normalizedSnapshotHash|bundleId|snapshotId" apps/web/src/app/api/bundles/upload/route.ts`
- `rg -n "scanArchive|server-scan" packages/core/src`
</acceptance_criteria>
<verify>
  <automated>npx nx test core && npx nx build web</automated>
</verify>
<done>Upload route compiles; core exposes server scan; tests cover idempotency or validation failures.</done>
</task>

<task type="auto">
<name>Task 3: GET list + GET download stream</name>
<files>apps/web/src/app/api/bundles/route.ts, apps/web/src/app/api/bundles/[bundleId]/snapshots/route.ts, apps/web/src/app/api/bundles/[bundleId]/snapshots/[snapshotId]/download/route.ts, tests/api-upload.test.ts</files>
<objective>
Implement API-02: list caller's bundles (and snapshots as needed for restore); download returns **binary stream** of stored zip with correct `Content-Type` / `Content-Disposition`; verify **ownership** before Storage read (D-05, D-08 server stream).
</objective>
<read_first>
- `supabase/migrations/*.sql` (table shapes)
- `.planning/phases/02-backend-private-backup/02-CONTEXT.md` (D-08)
</read_first>
<action>
1. **`GET apps/web/src/app/api/bundles/route.ts`**: return JSON list for `owner_user_id = user.id` (fields: id, displayName, createdAt, latest snapshot summary).
2. **`GET apps/web/src/app/api/bundles/[bundleId]/snapshots/route.ts`** (if not folded into list): return snapshots for that bundle id when owned.
3. **`GET .../download/route.ts`**: validate bundle + snapshot belongs to user; `storage.download` or stream; map to `NextResponse` streaming body.
4. Extend tests or add **`tests/api-list-download.test.ts`** with mocks proving **403/404** when `bundleId` not owned.
</action>
<acceptance_criteria>
- `rg -n "GET|NextResponse|download|storage" apps/web/src/app/api/bundles`
- `npx nx build web`
</acceptance_criteria>
<verify>
  <automated>npx nx build web && rg -n "download" apps/web/src/app/api/bundles</automated>
</verify>
<done>List and download routes exist; ownership enforced in code paths.</done>
</task>

</tasks>

<verification>
With staging credentials: upload → list shows bundle → download bytes match uploaded archive hash or size; unauthenticated requests get 401.
</verification>

<success_criteria>
1. API-01 satisfied: authenticated upload + validation + structured 4xx + remediation.
2. API-02 satisfied: list + download for owner only; server-mediated Storage read.
3. No client exposure of service role; core validation/scan not duplicated in `apps/web`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-private-backup/02-02-SUMMARY.md`
</output>
